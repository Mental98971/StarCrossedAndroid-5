<?xml version="1.0" encoding="utf-8"?>
<!--
   updatePeriodMillis is a slow (30 min) system-triggered safety-net refresh
   only. The real-time path is StarCrossedWidget.pushUpdate(), called
   directly by StarCrossedMediaService every time playback state changes —
   this periodic timer exists purely so a widget that was added while
   nothing was playing still eventually reflects reality, not as the
   primary update mechanism (Android also enforces a practical minimum
   period of 30 minutes for this value regardless of what's requested).
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="80dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:initialLayout="@layout/widget_star_crossed"
    android:previewImage="@drawable/widget_preview"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_description"
    android:updatePeriodMillis="1800000" />

